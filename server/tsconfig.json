{
  "compilerOptions": {
    "target": "es6",
    "module": "commonjs",
    "rootDir": "./src", 
    "outDir": "./build",
    "esModuleInterop": true, 
    "strict": true
  }
}

// to use 3rd party libararies such as node, those libraries should also have dynamic types.
// this is where TypeScript allows the creation of declaration files.
// npm install -D @types/node @types/express (for TS to work with node and express)